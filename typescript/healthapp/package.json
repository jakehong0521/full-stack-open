{
  "name": "healthapp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "calculateBmi": "tsc --noEmit && node bmiCalculator.ts",
    "calculateExercises": "tsc --noEmit && node exerciseCalculator.ts",
    "dev": "concurrently \"tsc --watch\" \"node --watch index.ts\"",
    "start": "node index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.34.5",
  "type": "module",
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^26.2.0",
    "concurrently": "^10.0.5"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
